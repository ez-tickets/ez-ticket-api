use std::sync::Arc;

pub struct AppModule {
    inner: Arc<Handler>
}

pub struct Handler {
    
}

impl Handler {
    pub fn new() -> Handler {
        
        // let writer = WriteProtocol::new();
        todo!()
    }
}